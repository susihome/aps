# APS Project Rules & Standards

## 1. 技术栈约束 (Tech Stack)
- **Backend**: Spring Boot 3.4+, JDK 21 (使用虚拟线程提升求解性能)。
- **Solver**: Timefold Solver (必须使用 Constraint Streams API)。
- **Database**: PostgreSQL，主键必须使用 `java.util.UUID`。
- **Frontend**: Vue 3 + Tailwind CSS + Frappe-Gantt。

## 2. 命名与模型规范 (Domain Modeling)
- 遵循 **ISA-95** 标准：设备称为 `Resource`，工序称为 `Operation`，任务称为 `Task`。
- 所有 DTO 和 Entity 必须包含 `createTime` 和 `updateTime`。

## 3. 代码质量要求 (Code Quality)
- **TDD (测试驱动开发)**：在编写 Solver 逻辑前，必须先写 `ConstraintVerifier` 测试用例。
- **并发安全**：由于排产涉及大量计算，共享变量必须使用 `final` 或原子类。
- **异常处理**：全局统一异常捕获，返回格式固定为 RuoYi-Vue-Plus 的 `AjaxResult`。

## 4. UI 规范 (UI/UX)
- 甘特图拖拽必须触发异步校验，逻辑：`UI Drag -> Backend Validate -> UI Update/Revert`。
- 颜色体系：明亮的蓝色主色调 (#2563EB)，任务条根据状态（正常、逾期、欠料）显示不同颜色。

## 5. Spring Boot 后端规范 (Backend Rules)

- **构造器注入**：必须使用构造器注入（Constructor Injection），禁止字段级 `@Autowired`。依赖字段声明为 `private final`。
- **DTO 隔离**：API 层只暴露 DTO，禁止直接返回 JPA 实体。使用 Bean Validation（`@Valid`、`@NotNull`）验证请求体。
- **全局异常处理**：使用 `@RestControllerAdvice` + `@ExceptionHandler`，返回统一 `AjaxResult`，禁止暴露堆栈或内部路径。
- **事务管理**：`@Transactional` 只加在 Service 方法上；只读操作加 `readOnly = true`。
- **日志**：使用 SLF4J 参数化消息（`logger.info("Processing {}", id)`），禁止字符串拼接。
- **配置外部化**：使用 `@ConfigurationProperties` 绑定配置，禁止在代码或配置文件中硬编码密钥/密码。
- **组件注解**：按语义选择 `@Service` / `@Repository` / `@RestController`，禁止滥用 `@Component`。
- **禁止**：字段注入、跳过输入验证、使用 Spring Boot 2.x 废弃 API（如 `WebSecurityConfigurerAdapter`）、`application.yml` 中存储 Secret。

## 6. Vue 3 前端规范 (Frontend Rules)

- **Composition API**：必须使用 `<script setup lang="ts">`；除非有明确原因否则禁止 Options API。
- **SFC 顺序**：`<script>` → `<template>` → `<style>`。
- **数据流**：Props down，Events up；仅对真正的双向绑定使用 `v-model`；深层依赖共享用 `provide/inject`。
- **组件拆分**：满足以下任意条件必须拆分：① 超过 1 个职责；② 3+ 个独立 UI 区块；③ 重复的模板块。
- **Composables**：可复用/有状态/有副作用的逻辑提取为 `useXxx()` composable。
- **类型声明**：`defineProps`、`defineEmits` 必须有显式 TypeScript 类型。
- **模板约束**：禁止在模板中执行复杂逻辑；分支/派生计算移到 `computed` 或 `<script>`。
- **性能**：先实现功能，功能正确后再做性能优化（`v-once`、`v-memo`、虚拟滚动）。

## 7. TypeScript 编码风格 (TypeScript Style)

- 导出函数必须有显式参数类型和返回类型；内部局部变量允许类型推断。
- 禁止 `any`；外部/不可信输入使用 `unknown` 并做安全 narrowing。
- 对象更新使用展开运算符（不可变更新），禁止 in-place mutation。
- `interface` 用于可扩展对象形状；`type` 用于联合/交叉/映射类型。
- 优先字符串字面量联合类型代替 `enum`（除非需要互操作）。
- 禁止在生产代码中出现 `console.log`，使用统一日志方案。
- 错误处理：`async/await` + `try/catch`，catch 参数类型为 `unknown`，安全提取 message。

## 8. 安全规范 (Security)

提交前强制 checklist：
- [ ] 无硬编码 Secret（API Key、密码、Token）——使用环境变量
- [ ] 所有用户输入已验证（后端 `@Valid`，前端 Zod 或类似方案）
- [ ] SQL 注入已防范（Spring Data JPA / 参数化查询）
- [ ] 错误响应不暴露内部信息
- [ ] 认证/授权已覆盖
- [ ] 发现安全问题立即停止，先修复后继续

## 9. 测试规范 (Testing)

- 最低测试覆盖率：**80%**。
- TDD 工作流（Solver 逻辑强制）：RED（写测试）→ GREEN（最小实现）→ IMPROVE（重构）。
- 三类测试全覆盖：Unit（JUnit 5 + Mockito）、Integration（`@SpringBootTest` / Testcontainers）、E2E（Playwright）。
- Controller 层用 `@WebMvcTest`；Repository 层用 `@DataJpaTest`。
- 禁止删除或弱化已有测试用例。

## 10. Git 工作流 (Git Workflow)

- Commit 格式：`<type>: <description>`
  - 类型：`feat` / `fix` / `refactor` / `docs` / `test` / `chore` / `perf` / `ci`
- PR 必须包含完整变更摘要和测试计划（`git diff <base>...HEAD`）。

## 11. 通用代码规范 (General Code Quality)

- **DRY**：提取重复逻辑，禁止复制粘贴代码块。
- **KISS**：优先最简方案；无法在 30 秒内理解的代码需要重构。
- **YAGNI**：不写"以备将来"的功能或接口。
- 函数 < 50 行；文件 < 800 行（常规 200–400 行）；嵌套深度 ≤ 4 层。
- 使用 Guard Clause 提前 return，避免深层嵌套。
- 魔法数字/字符串必须定义为有名常量。
- 注释说明"为什么"，不说"做什么"（代码自文档化）。
- 按 feature/domain 组织代码，不按 layer 组织。
- API 响应统一格式：`{ success, data?, error?, meta? }`。